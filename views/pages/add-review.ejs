<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <%- include('../components/header') %>
        <main>
            <div class="card" style="max-width: 600px; margin: auto;">
                <h2 class="mb-3"><%= title %></h2>
                <p><strong>Author:</strong> <%= book.author %></p> 
                
                <form action="/reviews/add/<%= book.id %>" method="POST">
                    

                    <div class="form-group">
                        <label for="star" class="form-label">Rating (0.0 - 5.0)</label>

                        <input type="number" id="star" name="star" class="form-control" 
                               min="0" max="5" step="0.5" required> 
                    </div>
                    

                    <div class="form-group">
                        <label for="detail" class="form-label">Review</label>
                        <textarea id="detail" name="detail" class="form-control" rows="5" required></textarea>
                    </div>

                    <button type="submit" class="btn btn-primary">Submit Review</button>
                </form>
            </div>
        </main>
    </div>
</body>
</html>